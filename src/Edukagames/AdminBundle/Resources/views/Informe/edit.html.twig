<h1>Informe edit</h1>

<form action="{{ path('informe_update', { 'id': informe.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
		<a href="{{ path('informe', { 'id': informe.Alumno.id }) }}">
            Volver a la lista
        </a>
    </li>
</ul>

login:
    pattern: /
    defaults: { _controller: 'UserBundle:Default:login' }
 
login_check:
    pattern: /login_check
 
logout:
    pattern: /logout

#pagina principal de alumno
user_index:
    pattern: /juegos/
    defaults: { _controller: 'UserBundle:Default:index'}
#   

UserBundle_alumno:
    resource: "@UserBundle/Resources/config/routing/alumno.yml"
    prefix: /alumno
    
alumnos_details:
    pattern: /alumno/{id}
    defaults: { _controller: 'UserBundle:Alumno:AlumnoDetails'}

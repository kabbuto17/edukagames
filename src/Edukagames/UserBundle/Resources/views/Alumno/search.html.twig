      	<form id="search"  action={{ path('alumno_search') }} method="post" {{ form_enctype(form) }}>
      		{{ form_widget(form.search, { 'attr': {'placeholder': 'Buscar'} }) }}
      		{{ form_widget(form._token) }}
      	</form>
   		<div>
			<div id="result">
   		      	{% for res in result %}
   		      		<p>
	   		      		<a href="{{ path('alumnos_details', {'id': res.id}) }}"><i class="icon-user"></i> {{ res.nombreCompleto }}</a>
   		      		</p>
   		      	{% endfor %}
			</div>
   		</div>
				
   		<script type="text/javascript">
  	/**
		$('#result').click(function (e){
			if( $(e.target).is('a') ){
				//alert(e.target.href);
				var url = e.target.href;
			$('#content').load(url);
			}
			return false;
   	   	});
   	*/
	//buscar como asociar al keyCode=13 que es el enter, si pulsas enter buscara mientras no...
   		$('#Search_search').bind('keyup', function() { $('#search').submit(); } );
		$('#search').submit(function(e) {
   		    var url = $(this).attr("action");
   		    $.ajax({
   		        type: "POST",
   		        url: url, // Or your url generator like Routing.generate('discussion_create')
   		        data: $(this).serialize(),
   		        dataType: "html",
   		        success: function(msg){
   		           $('#result').html($(msg).find("#result").html());
   		        }
   		    });
   		    console.log("buscando");
   		    return false;
   		});
		</script> 
	
{#
Borrar tiene q enviarse por post y editar ahora mismo se envia por get, 
tiene un rule_admin para que solo entre en admin aun asi...
#}

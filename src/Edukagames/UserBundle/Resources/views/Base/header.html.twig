<div>
	<div class="img-cabecera">
		<img src="{{ asset( 'bundles/user/img/eduka.jpg' ) }}">
	</div>
	<div>
	{% if is_granted('ROLE_USER') %}
	Bienvenido {{ app.user.userName }} <a href="{{ path('logout') }}">salir</a>
	{% endif %}
	</div>
</div>